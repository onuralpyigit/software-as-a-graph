Relation typing and QoS edge encoding are substitutes, not complements.
Their interaction is negative on all twelve held-out architectures (p = 0.0005).
Typing is worth +0.234 without a QoS edge channel and +0.035 with one.
Untrained QoS-weighted centrality is not significantly beaten by the learned model.
Zero-shot transfer fails on the real-world components that propagate failures.
