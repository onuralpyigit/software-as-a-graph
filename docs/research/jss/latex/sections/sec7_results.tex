\section{Results and Empirical Analysis}
\label{sec:7}

\subsection{RQ1: Graph Learning vs. Structural Baselines}
\label{sec:7.1}

Table~\ref{tab:5} presents the in-distribution held-out Spearman rank correlation ($\rho$) against simulated cascade impact $I^*(v)$ across all seven synthetic scenarios.

\begin{table}[htbp]
\centering
\small
\caption{In-distribution held-out Spearman rank correlation ($\rho$) against $I^*(v)$, reported as the mean over five seeds $\pm$ the standard deviation across those seeds; $n$ is the held-out Application count. Both learned graph models (GL, SaG) are evaluated under substrate and feature parity on the native multigraph; topological baselines operate on the flow projection. Each seed redraws the 60/20/20 split as well as the initialisation (Section~\ref{sec:6.3}). Read alongside the paired tests of Table~\ref{tab:6}, which use the scenario as the unit of analysis.}
\label{tab:5}
\resizebox{\linewidth}{!}{%
\begin{tabular}{lrcccc}
\toprule
\textbf{Scenario} & \textbf{$n$} & \textbf{Topo-BL} & \textbf{Topo-QoS} & \textbf{GL (Homogeneous)} & \textbf{SaG (Typed)} \\
\midrule
\textbf{AV System} & 16 & 0.297 {\scriptsize$\pm$0.366} & \textbf{0.723} {\scriptsize$\pm$0.252} & 0.698 {\scriptsize$\pm$0.244} & 0.649 {\scriptsize$\pm$0.121} \\
\textbf{Enterprise} & 60 & 0.415 {\scriptsize$\pm$0.116} & 0.805 {\scriptsize$\pm$0.059} & 0.886 {\scriptsize$\pm$0.048} & \textbf{0.891} {\scriptsize$\pm$0.037} \\
\textbf{Financial Trading} & 12 & 0.268 {\scriptsize$\pm$0.341} & 0.700 {\scriptsize$\pm$0.177} & \textbf{0.821} {\scriptsize$\pm$0.211} & 0.770 {\scriptsize$\pm$0.305} \\
\textbf{Healthcare} & 10 & $-$0.155 {\scriptsize$\pm$0.245} & \textbf{0.768} {\scriptsize$\pm$0.200} & 0.594 {\scriptsize$\pm$0.347} & 0.645 {\scriptsize$\pm$0.391} \\
\textbf{Hub-and-Spoke} & 14 & 0.285 {\scriptsize$\pm$0.257} & \textbf{0.401} {\scriptsize$\pm$0.318} & 0.331 {\scriptsize$\pm$0.424} & 0.297 {\scriptsize$\pm$0.601} \\
\textbf{IoT Smart City} & 40 & $-$0.058 {\scriptsize$\pm$0.152} & 0.071 {\scriptsize$\pm$0.179} & 0.603 {\scriptsize$\pm$0.343} & \textbf{0.842} {\scriptsize$\pm$0.088} \\
\textbf{Microservices} & 18 & 0.352 {\scriptsize$\pm$0.348} & \textbf{0.697} {\scriptsize$\pm$0.216} & 0.479 {\scriptsize$\pm$0.310} & 0.476 {\scriptsize$\pm$0.351} \\
\midrule
\textbf{Mean} & --- & \textbf{0.201} & \textbf{0.595} & \textbf{0.630} & \textbf{0.653} \\
\bottomrule
\end{tabular}%
}
\end{table}

\begin{table}[htbp]
\centering
\small
\caption{Paired Wilcoxon signed-rank tests across scenarios ($n = 7$, two-sided). At $n = 7$ the smallest attainable two-sided $p$ is $0.0156$, which is above $\alpha/5 = 0.010$; flagged results should be read as uncorrected and exploratory (Section~\ref{sec:6.3}).}
\label{tab:6}
\resizebox{\linewidth}{!}{%
\begin{tabular}{lrcrcl}
\toprule
\textbf{Comparison} & \textbf{$\Delta\rho$} & \textbf{Won} & \textbf{Wilcoxon $W$} & \textbf{$p$-value} & \textbf{Significance} \\
\midrule
\textbf{SaG vs. Topo-BL} & \textbf{+0.452} & 7/7 & 0.0 & \textbf{0.0156} & \textbf{Statistically Significant} ($p < 0.05$) \\
\textbf{Topo-QoS vs. Topo-BL} & \textbf{+0.394} & 7/7 & 0.0 & \textbf{0.0156} & \textbf{Statistically Significant} ($p < 0.05$) \\
\textbf{SaG vs. GL} & \textbf{+0.022} & 3/7 & 13.0 & 0.938 & Not significant (in-distribution parity) \\
\textbf{SaG vs. Topo-QoS} & \textbf{+0.058} & 3/7 & 11.0 & 0.688 & Not significant \\
\textbf{GL vs. Topo-QoS} & \textbf{+0.035} & 3/7 & 13.0 & 0.938 & Not significant \\
\bottomrule
\end{tabular}%
}
\end{table}

\subsubsection{Out-of-Distribution (LOSO) Generalization}

In inductive Leave-One-Scenario-Out (LOSO) cross-validation, models are evaluated on their capacity to predict cascading criticality over completely unseen system topologies:

\begin{table}[htbp]
\centering
\small
\caption{Inductive Leave-One-Scenario-Out (LOSO) evaluation, Application population, eight folds. Rows are grouped by role: training-free structural baselines, learned predictors under complete substrate and feature parity, and the RM/$Q(v)$ diagnostic reference of Section~\ref{sec:1.2}. Each fold score is the mean over the five seeds; \textbf{Fold $\sigma$} is the population standard deviation of those eight fold means, \textbf{Seed $\sigma$} is the median within-fold standard deviation across seeds (zero by construction for the deterministic baselines), and the 95\% interval is a non-parametric bootstrap over folds ($B = 2{,}000$).}
\label{tab:7}
\resizebox{\linewidth}{!}{%
\begin{tabular}{lccccccc}
\toprule
\textbf{Predictor / Reference} & \textbf{Mean LOSO $\rho$} & \textbf{95\% CI} & \textbf{Fold $\sigma$} & \textbf{Seed $\sigma$} & \textbf{Critical-Set $F_1@K$} & \textbf{Requires Training} \\
\midrule
\multicolumn{7}{l}{\textit{Training-free structural baselines}} \\
\textbf{Topo-BL} & 0.301 & $[0.210, 0.379]$ & 0.126 & --- & 0.363 & No \\
\textbf{Topo-QoS} & 0.571 & $[0.440, 0.688]$ & 0.181 & --- & 0.380 & No \\
\midrule
\multicolumn{7}{l}{\textit{Learned predictors (substrate and feature parity)}} \\
\textbf{GL (Homogeneous GNN)} & 0.363 & $[0.299, 0.421]$ & \textbf{0.089} & 0.326 & 0.341 & Yes \\
\textbf{SaG (Typed Heterogeneous)} & \textbf{0.608} & $[0.508, 0.702]$ & 0.143 & \textbf{0.083} & \textbf{0.414} & Yes \\
\midrule
\multicolumn{7}{l}{\textit{Diagnostic reference --- not a ranking model}} \\
\textbf{RM / $Q(v)$} & 0.195 & $[0.102, 0.279]$ & 0.130 & --- & 0.327 & No \\
\bottomrule
\end{tabular}%
}
\end{table}

Eight LOSO folds are reported, comprising the seven synthetic scenarios and the ATM case study from Section~\ref{sec:7.3}. In each fold, one scenario is held out for zero-shot testing while the model is trained exclusively on the remaining seven. All variants are evaluated on the identical Application node set per fold (Section~\ref{sec:6.3}); paired Wilcoxon tests are conducted across the eight folds. Per-fold evaluated populations range from 26 to 300 Application nodes, so $K = \text{round}(0.20\,|V_{\text{app}}|)$ ranges from 5 to 60. On $F_1@K$, SaG beats Topo-QoS in 4 of 8 folds ($W = 9.0$, $p = 0.469$) and GL in 8 of 8 ($W = 0.0$, $p = 0.0078$): the learned model separates itself from untyped learning on this metric, but not from the training-free QoS baseline.

\noindent\textbf{Label-noise ceiling.} These correlations are bounded by the reproducibility of the target they are scored against. Re-running the ground-truth oracle across the five seeds gives a test--retest rank correlation of $0.97$--$1.00$ in six of the seven synthetic scenarios; the exception is Microservices at $\rho = 0.807$, which sets the effective ceiling for cross-fold means. SaG's $\rho = 0.608$ therefore recovers roughly three quarters of the attainable signal, and no predictor in Table~\ref{tab:7} can exceed the reproducibility of its own labels. Top-$K$ critical sets are the noisier construct: their cross-seed Jaccard falls to $0.44$ (Microservices) and $0.71$ (AV, Financial Trading), which is part of why the $F_1@K$ margins above are less stable than the ranking margins.

\noindent\textbf{Key Insights for RQ1:}
\begin{enumerate}
    \item \textbf{The typed predictor is the best learned configuration overall.} SaG leads every other evaluated predictor on both metrics ($\rho = 0.608$, $F_1@K = 0.414$). Crucially, under complete substrate and feature parity with homogeneous learning, SaG achieves a ranking margin that excludes zero ($+0.246$ over GL, 95\% CI $[+0.158, +0.329]$; 8/8 folds won, $W = 0.0$, $p = 0.0078$).
    \item \textbf{Critical-set identification separates learned models from homogeneous baselines, while matching the untrained QoS baseline.} SaG improves $F_1@K$ over GL by $+0.073$ ($0.414$ vs.\ $0.341$), but against Topo-QoS the margin is $+0.034$ ($0.414$ vs.\ $0.380$), won in 4 of 8 folds with per-fold differences alternating in sign ($W = 9.0$, $p = 0.469$). Critical-set identification is not a demonstrated advantage of the learned model over the QoS-weighted baseline on this corpus.
    \item \textbf{Ranking Boundary against training-free baselines.} Against \emph{Topo-QoS}, SaG's ranking margin is $+0.037$ with a 95\% bootstrap interval of $[-0.079, +0.162]$ that comfortably contains zero, won in 5 of 8 folds ($p = 0.64$). The interval, not the $p$-value, is the informative statement here: at this sample size the data are consistent with the learned model being anywhere from slightly worse to moderately better than the baseline. On out-of-distribution ranking, heterogeneous graph learning matches rather than outperforms a well-constructed QoS baseline, and the critical-set margin above is likewise not significant. The case for training a model therefore rests on the qualitative capabilities the baseline cannot supply --- actionable explanations, per-relationship edge criticality, and multi-task quality attributions (Section~\ref{sec:8.1}).
    \item \textbf{The explanation layer is weakly predictive, not noise.} RM/$Q(v)$ achieves $\rho = 0.195$ under distribution shift --- outperforming simple topological baselines while providing interpretable architectural diagnostics without training (Section~\ref{sec:5}).
\end{enumerate}

Figure~\ref{fig:5} provides an overview of these results, summarizing out-of-distribution rank correlation, critical-set identification, and cross-oracle agreement.

\begin{figure}[htbp]
\centering
\includegraphics[width=\linewidth]{figures/Figure_5}
\caption{Results at a glance, evaluated on the Application population. \textbf{(A)} Out-of-distribution rank correlation per variant across eight LOSO folds (whiskers denote $\sigma$). \textbf{(B)} Critical-set identification at $K = 20\%$. \textbf{(C)} Pairwise agreement across the three simulation oracles.}
\label{fig:5}
\end{figure}

\subsection{RQ2: Value of Typed Heterogeneity}
\label{sec:7.2}

To evaluate the specific contribution of node and edge typing, we contrast the relation-specific Heterogeneous Graph Transformer (SaG) against the homogeneous Graph Attention Network (GL) on the shared native multigraph substrate under complete substrate and feature parity:
\begin{itemize}
    \item \textbf{In-Distribution Fitting (Table~\ref{tab:5}):} On familiar architectures, homogeneous and heterogeneous models exhibit comparable fitting capacity ($\rho = 0.653$ for SaG vs.\ $0.630$ for GL; $\Delta\rho = +0.022$, $W = 13.0$, $p = 0.938$, won in 3/7 scenarios). On known topologies, homogeneous GNNs partially compensate for missing relation types by leveraging per-type input embeddings, structural degree signatures, and continuous edge attributes.
    \item \textbf{Out-of-Distribution Generalization (LOSO, Table~\ref{tab:7}):} Under inductive distribution shift to completely unseen architectures, homogeneous message passing degrades while typed heterogeneous learning generalizes robustly. SaG outperforms homogeneous GL by \textbf{$+0.246$} ($\rho = 0.608$ vs.\ $0.363$, winning \emph{all eight} folds, $W = 0.0$, $p = 0.0078$; 95\% CI $[+0.158, +0.329]$).
\end{itemize}

\noindent\textbf{Relational Typing as an Inductive Generalization Bias.} Read together, the in-distribution and out-of-distribution results reveal a fundamental principle: \emph{the demonstrated value of typed message passing is an inductive generalization property rather than an in-distribution fitting advantage.} When trained and evaluated on the same system distribution, both homogeneous and heterogeneous architectures have sufficient parameter capacity to fit cascading criticality patterns. However, structural degree distributions and local neighbor counts fail to transfer zero-shot across distinct architectural topologies. In contrast, relation-specific parameters (e.g., distinguishing \texttt{PUBLISHES\_TO} message dissemination from \texttt{RUNS\_ON} host placement) capture invariant causal semantics of failure propagation that persist across previously uncharacterized systems.

\subsubsection{Empirical Edge-Removal Analysis}

We further evaluated edge criticality by simulating the removal of individual edges while keeping both endpoint components operational. The 50 highest-ranked candidate edges in the \texttt{av\_system} topology comprised 35 \texttt{RUNS\_ON}, 11 \texttt{CONNECTS\_TO}, 3 \texttt{SUBSCRIBES\_TO} and 1 \texttt{PUBLISHES\_TO} edge. Exactly 4 removals produced non-zero downstream cascade impact, and they were exactly the 4 communication channels: every host-placement and inter-host network edge in the pool was inert, while every publish--subscribe edge in it was not. The effect is nonetheless small in magnitude --- the largest single-edge impact was $0.00504$ --- so the finding is that severing a message channel degrades reachability slightly whereas relocating a component or dropping a host link does not, not that any one channel is load-bearing. With only 4 communication edges in the candidate pool this is a descriptive observation on one topology rather than a tested claim, and we report it as such.

\subsection{RQ3: Ablations and Sensitivity Analysis}
\label{sec:7.3}

\noindent\textbf{Role of $\rho$ in Sensitivity Sweeps.} The parameter sweeps below utilize RM's rank correlation against $I^*(v)$ strictly as a \emph{sensitivity probe} to quantify parameter leverage relative to between-model margins.

\subsubsection{QoS Feature Ablation}
\label{sec:7.3.1}
\label{sec:7.3.qos}

To isolate the specific empirical contribution of the 7-dimensional continuous-categorical QoS edge features (Section~\ref{sec:4.1.1}), we evaluated an un-augmented ablation of SaG where edge features contain only scalar coupling and relation one-hot encodings. 

Ablating QoS edge encodings produces a severe drop in out-of-distribution transfer: mean LOSO rank correlation falls from $\rho = 0.608$ down to $0.439$ ($\Delta\rho = -0.169$, with the full model winning 7 of 8 folds, $W = 2.0$, $p = 0.0156$; 95\% bootstrap CI $[+0.046, +0.278]$). Similarly, ablating QoS features from the homogeneous architecture causes GL to collapse from $\rho = 0.363$ to $0.086$. Furthermore, the QoS encoding substantially stabilizes model training across random initializations: median within-fold standard deviation across the five seeds is $0.083$ for full SaG compared to $0.284$ for the un-augmented ablation (narrowing from $\pm 0.490$ to $\pm 0.053$ on the AV fold). While structural degree signatures vary dramatically across unseen architectures, declared middleware QoS contracts (\texttt{RELIABLE}, \texttt{TRANSIENT\_LOCAL}) preserve invariant delivery semantics everywhere. The full per-scenario in-distribution and LOSO ablation results are recorded in the replication package.

\subsubsection{Topic-Weight Coefficients (\texorpdfstring{$\beta$, $\alpha$, $\psi$}{beta, alpha, psi})}
\label{sec:7.3.topicw}

We swept $(\beta, \alpha, \psi)$ over a grid spanning the entire simplex to evaluate sensitivity to topic coupling weights, with results detailed in Table~\ref{tab:8b}:

\begin{table}[htbp]
\centering
\small
\caption{Sensitivity of topic-weight ordering and downstream rank correlation against $I^*(v)$ to coefficients $(\beta, \alpha, \psi)$ (Application population, mean over 7 scenarios, 375 topics).}
\label{tab:8b}
\begin{tabular}{lccc}
\toprule
\textbf{$(\beta, \alpha, \psi)$} & \textbf{$\rho$ of $w(t)$ vs.\ shipped} & \textbf{Topo-QoS $\rho$} & \textbf{RM $\rho$} \\
\midrule
$(0.75, 0.15, 0.10)$ \emph{shipped} & $1.000$ & $0.604$ & $0.267$ \\
$(0.90, 0.05, 0.05)$ & $0.966$ & $0.600$ & $0.273$ \\
$(0.85, 0.15, 0.00)$ & $0.950$ & $0.608$ & $0.268$ \\
$(1.00, 0.00, 0.00)$ & $0.852$ & $0.618$ & $0.268$ \\
$(0.60, 0.20, 0.20)$ & $0.970$ & $0.604$ & $0.261$ \\
$(0.50, 0.25, 0.25)$ & $0.942$ & $0.603$ & $0.259$ \\
$(\tfrac13, \tfrac13, \tfrac13)$ \emph{uniform} & $0.870$ & $0.608$ & $0.256$ \\
\midrule
\textbf{Spread over grid} & — & $\mathbf{0.018}$ & $\mathbf{0.017}$ \\
\bottomrule
\end{tabular}
\end{table}

\noindent Topic-weight coefficients are not load-bearing: the ordering of $w(t)$ never falls below $\rho = 0.852$ against the default, and downstream rank correlations vary by at most $0.018$ (Topo-QoS) and $0.017$ (RM).

\subsubsection{Intra-Dimension AHP Weight Shrinkage}

We evaluated the sensitivity of the RM attribution baseline across shrinkage parameter $\lambda \in [0, 1]$, blending internal term weights from a uniform prior ($\lambda = 0$) to raw AHP judgment ($\lambda = 1$), as summarized in Table~\ref{tab:8}:

\begin{table}[htbp]
\centering
\small
\caption{Sensitivity of RM rank correlation against $I^*(v)$ to AHP shrinkage parameter $\lambda$ (Application population, mean over 7 scenarios).}
\label{tab:8}
\begin{tabular}{lccccc}
\toprule
\textbf{$\lambda$ Setting} & \textbf{0.00 (Uniform)} & \textbf{0.50} & \textbf{0.70 (Default)} & \textbf{0.80} & \textbf{1.00 (Raw AHP)} \\
\midrule
\textbf{Mean Rank Correlation ($\rho$)} & \textbf{0.348} & $0.291$ & $0.267$ & $0.256$ & $0.232$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.62\linewidth]{figures/Figure_4}
\caption{Sensitivity of RM composite rank correlation against $I^*(v)$ across AHP shrinkage $\lambda$ (Application population, mean over 7 scenarios).}
\label{fig:4}
\end{figure}

\noindent Rank correlation decreases monotonically as weights transition from uniform toward raw AHP ($\rho = 0.348 \to 0.232$, Figure~\ref{fig:4}). Elicited AHP weights provide transparent, auditable domain attribution rather than optimizing rank correlation. We retain $\lambda = 0.70$ on that basis.

\subsubsection{Joint Sensitivity Across All Ten Weight Constants}
\label{sec:7.3.global}

To assess interactions, we swept all ten weight constants jointly across six scenarios using Morris elementary-effects screening \cite{morris1991factorial,campolongo2007effective} (a computationally efficient alternative to variance-based global sensitivity analysis \cite{saltelli2008global,sobol1993sensitivity}), with factor influences reported in Table~\ref{tab:8e}:

\begin{table}[htbp]
\centering
\small
\caption{Morris elementary-effects screening \cite{morris1991factorial,campolongo2007effective} across ten weight constants, ranked by influence ($\mu^*$) on mean $\rho$ (6 scenarios, 10 trajectories, 110 evaluations).}
\label{tab:8e}
\begin{tabular}{lcc}
\toprule
\textbf{Factor} & \textbf{$\mu^*$} & \textbf{$\sigma$} \\
\midrule
$\lambda$ (AHP shrinkage) & $0.124$ & $0.077$ \\
$r_\alpha$ & $0.096$ & $0.035$ \\
$\alpha$ & $0.019$ & $0.034$ \\
$w_{\text{rel}}$ & $0.019$ & $0.018$ \\
$\beta$ & $0.017$ & $0.023$ \\
$w_{\text{dur}}$ & $0.014$ & $0.017$ \\
$w_{\text{prio}}$ & $0.013$ & $0.018$ \\
$\psi$ & $0.012$ & $0.015$ \\
$p$ (power-mean exponent) & $0.005$ & $0.005$ \\
$\gamma$ (fan-out) & $0.001$ & $0.002$ \\
\bottomrule
\end{tabular}
\end{table}

\noindent Morris screening confirms that $\lambda$ and $r_\alpha$ are the primary drivers, while topic and QoS sub-weights reside in a modest band ($\mu^* \in [0.012, 0.019]$). Dirichlet simplex sampling over 100 draws confirms tight stability: mean $\rho = 0.243$ (standard deviation $0.006$, 90\% interval $[0.231, 0.253]$, mean top-20\% Jaccard $0.825$).

\subsubsection{Convergent Validity Over Simulation Oracles}

We evaluated inter-oracle agreement across $I^*(v)$ (\texttt{FaultInjector}), $I_{\text{comp}}(v)$ (\texttt{FailureSimulator}), and $I_{\text{dyn}}(v)$ (\texttt{MessageFlowSimulator}) over seven scenarios, summarized in Table~\ref{tab:8c}:

\begin{table}[htbp]
\centering
\small
\caption{Inter-oracle agreement across simulation paradigms (chance top-$K$ Jaccard is $0.111$).}
\label{tab:8c}
\begin{tabular}{lcccc}
\toprule
\textbf{Oracle pair} & \textbf{Mean $\rho$ (range)} & \textbf{Mean $\tau$} & \textbf{Jaccard@$K$} & \textbf{Tie-robust} \\
\midrule
$I_{\text{dyn}}$ vs.\ $I^*$ & $\mathbf{0.883}$ ($0.756$--$0.972$) & $0.745$ & $0.424$ & $0.419$ \\
$I_{\text{comp}}$ vs.\ $I^*$ & $0.468$ ($0.171$--$0.677$) & $0.349$ & $0.307$ & $0.312$ \\
$I_{\text{comp}}$ vs.\ $I_{\text{dyn}}$ & $0.465$ ($0.121$--$0.658$) & $0.348$ & $0.313$ & $0.313$ \\
\bottomrule
\end{tabular}
\end{table}

\noindent Ordering converges strongly between the queue-flow simulator and topological cascade oracle ($\rho = 0.883$). Top-$K$ set agreement is more conservative (Jaccard $0.42$), bounded by discrete threshold sensitivity in non-linear cascades.

\subsubsection{Domain-Specific Weighting and Threshold Sensitivity}

Sweeping composite reliability weight $w_R \in [0, 1]$ moves mean $\rho$ by only $0.024$ ($0.341 \to 0.365$), with mean Kendall correlation $\tau = 0.974$ between domain and static rankings. Within that narrow band the domain-derived weighting does not improve on the alternatives it replaces --- mean $\rho$ is $0.347$ (domain-derived), $0.349$ (equal), and $0.353$ (static) --- so domain reweighting should be understood as an attributional device that expresses criticality in stakeholder terms, not as a ranking-accuracy mechanism. No weighting confined to this parameter could move $\rho$ appreciably. Sweeping cascade propagation thresholds revealed higher sensitivity ($\Delta\rho = 0.102$), with ranking performance plateauing above $0.35$.

\subsubsection{Anti-Pattern Detection and Node-Type Stratification}

Validated against $I_{\text{comp}}(v)$, the rule-based anti-pattern catalog reaches mean precision $0.239$ and recall $0.900$ ($F_1 = 0.378$), but the recall must be read against a flag rate of $94.2\%$: the 18 active detectors implicate almost everything, so recall near $0.9$ is close to what indiscriminate flagging yields and precision $0.239$ is roughly the base rate. Chance-corrected agreement confirms this and worsens with scale --- over the ATM sweep (29 to 444 components, five seeds) Cohen's $\kappa$ falls monotonically from $0.118$ to $-0.045$, i.e.\ to no better than chance on the largest graph. We report the catalog as a coarse, deliberately over-inclusive triage filter, \emph{not} a validated detector; every quantitative critical-set claim in this paper rests on the ranking models of Sections~\ref{sec:7.1}--\ref{sec:7.2} instead. A nineteenth detector, \texttt{DEEP\_PIPELINE}, is excluded outright: enumerating every simple source-to-sink path, it emitted $247{,}761$ findings without terminating on a 29-component fixture.

\noindent\textbf{Stratification vs. Pooling:} Measured against the composite oracle $I_{\text{comp}}(v)$ over the eight scenarios of the detection benchmark, stratified RM rank correlations are $\rho = 0.503$ (Application, 8 scenarios), $0.395$ (Broker, 6 scenarios), and $0.142$ (Node, 8 scenarios), while pooled correlation across all types is $\rho = 0.028$. These figures are not comparable to the RM values elsewhere in this paper and should not be read as the same quantity: Table~\ref{tab:8}'s $\rho = 0.267$ scores RM in-sample against $I^*(v)$ over the seven synthetic scenarios, and Table~\ref{tab:7}'s $\rho = 0.195$ scores it zero-shot against $I^*(v)$ over the eight LOSO folds. Oracle, corpus and protocol all differ; the contrast drawn here is strictly between the stratified and pooled readings of one benchmark. Pooling node types triggers Simpson's paradox by mixing distinct base rates. Classifying components within node types changes the criticality tier of $62.8\%$ of components (with $19.0\%$ crossing the critical boundary), confirming that evaluation and gating must operate strictly per-type. On that same pooled benchmark, unweighted degree centrality attains $\rho = 0.166$ and $F_1 = 0.413$ against RM's $0.028$ and $0.349$ --- a further reason to read $Q(v)$ as an attribution instrument rather than as a ranking model.

\subsubsection{HGT Attention Weight Analysis}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.86\linewidth]{figures/Figure_3}
\caption{Relational attention extracted from the trained HGT on the ATM case study. Peak attention focuses on \texttt{USES} (Application $\to$ Library) and \texttt{ROUTES} (Broker $\to$ Topic) channels.}
\label{fig:3}
\end{figure}

Aggregated by relation type over the ATM case study, first-layer mean attention orders \texttt{USES} into libraries ($0.227$, $0.215$) above \texttt{ROUTES} ($0.194$), \texttt{SUBSCRIBES\_TO} ($0.176$), \texttt{PUBLISHES\_TO} ($0.163$) and \texttt{RUNS\_ON} ($0.153$), with the same ordering to within $0.02$ in layers two and three. Two cautions bound what this supports. The spread across all eight relation types is narrow ($0.15$--$0.23$), so this is a tendency, not a separation; and because $\alpha$ is a softmax over each destination's incoming edges, mean $\alpha$ is driven substantially by in-degree --- the top-ranked \texttt{Library}$\to$\texttt{Library} relation carries 4 edges, and the largest weight in the graph ($\alpha_{uv} = 1.00$) sits at a destination of in-degree one, where $\alpha = 1$ holds by construction. Figure~\ref{fig:3} is therefore a qualitative illustration on one topology at one seed without a significance test, not evidence for why typing helps.

\subsection{RQ4: Real-World Distributed Architecture Validation}
\label{sec:7.4}

We evaluated SaG zero-shot on three open-source distributed systems transcribed from their public repositories by dedicated architectural adapters, with results presented in Table~\ref{tab:9}. We note the scope of this evaluation before reporting it. Online Boutique is a vendor demonstration application and Train-Ticket an academic benchmark, so these are reference implementations rather than production deployments; each is an abstraction of its upstream repository rather than a complete transcription; and their labels come from the same simulation oracle used throughout, so what follows tests topological transfer, not agreement with observed field failures. Two further limits bound what this table can support. First, only the typed architecture (SaG) was evaluated here: no homogeneous or QoS-weighted baseline was run on these systems, so the typing claim of Section~\ref{sec:7.2} is \emph{not} tested outside our own generator anywhere in this study, and should be read as a synthetic-corpus result until a matched comparison on non-generated architectures is available. Second, the labels are strongly tied at zero --- 13 of 32 Applications in Autoware, 14 of 22 in Cloud Microservices and 27 of 41 in Train-Ticket carry zero simulated impact (the complement of the ``Impactful Apps'' column). Spearman $\rho$ over a population that is 41--64\% tied is dominated by how those ties are handled; the values below use midrank ties throughout, and the highest correlation in the table ($\rho = 0.778$) rests on the population with the largest tied block, so it is the least, not the most, informative entry.

\begin{table}[htbp]
\centering
\small
\caption{Zero-shot transfer evaluation on open-source reference systems (held-out, without fine-tuning). The evaluated model is SaG (typed, with structural edge encodings where open-source manifests lack declared QoS contracts); $\pm$ denotes the standard deviation over the five training seeds $\{42, 123, 456, 789, 2024\}$. ``Gain vs.\ Deg'' is $|\rho_{\text{SaG}}| - |\rho_{\text{deg}}|$. Ground truth is simulator-derived, as elsewhere in this study; these are not observed production incidents.}
\label{tab:9}
\resizebox{\linewidth}{!}{%
\begin{tabular}{lcccccccc}
\toprule
\textbf{Real-World Architecture} & \textbf{$|V|$} & \textbf{$|V_{\text{app}}|$} & \textbf{Spearman $\rho$} & \textbf{Kendall $\tau$} & \textbf{App $F_1@K$} & \textbf{Pooled $F_1@K$} & \textbf{Impactful Apps} & \textbf{Gain vs.\ Deg} \\
\midrule
\textbf{Autoware.universe (ROS 2)} & 75 & 32 & \textbf{0.685 $\pm$ 0.010} & 0.513 & 0.333 & 0.800 & 19 / 32 & +0.357 \\
\textbf{Cloud Microservices Mesh} & 60 & 22 & \textbf{0.778 $\pm$ 0.001} & 0.639 & 0.500 & 1.000 & 8 / 22 & +0.014 \\
\textbf{Train-Ticket Booking Mesh} & 90 & 41 & \textbf{0.759 $\pm$ 0.001} & 0.605 & 0.625 & 1.000 & 14 / 41 & +0.264 \\
\bottomrule
\end{tabular}%
}
\end{table}

\noindent\textbf{Key Insights for RQ4:}
\begin{enumerate}
    \item \textbf{Strong Real-World Zero-Shot Transfer:} Trained exclusively on synthetic scenarios, SaG achieves high zero-shot rank correlation on Cloud Microservices ($\rho = 0.778$), Train-Ticket ($\rho = 0.759$), and Autoware.universe ($\rho = 0.685$), indicating that relation-specific graph representations transfer to architectures outside the generator's distribution. Because the comparison in this table is against degree centrality alone, it does not establish transfer superiority over the stronger \texttt{Topo-QoS} baseline of Table~\ref{tab:7}.
    \item \textbf{Stratified vs.\ Pooled Critical Set Identification:} On the stratified Application population ($V_{\text{app}}$ with $K = \text{round}(0.20 \cdot |V_{\text{app}}|)$), SaG identifies the top-20\% critical services with $F_1@K = 0.333$ in Autoware, $0.500$ in Cloud Microservices, and $0.625$ in Train-Ticket. When evaluated across the pooled multigraph ($K = \text{round}(0.20 \cdot |V|)$), the pooled $F_1@K$ reaches $0.800$, $1.000$, and $1.000$, respectively. This disparity highlights the base-rate phenomenon discussed in Section~\ref{sec:6.3}: unmeasured passive infrastructure entities (Topics, Nodes, Libraries) exhibit constant zero failure impact under process crash simulation. While the pooled metric confirms that SaG cleanly separates critical active services from inert infrastructure, reporting stratified $V_{\text{app}}$ metrics provides the more rigorous, non-inflated evaluation.
    \item \textbf{The framework's own acceptance gates do not pass on any of the three systems.} SaG ships a five-condition release gate ($\rho \ge 0.75$, $F_1 \ge 0.65$, SPOF $F_1 \ge 0.60$, fault-tolerance ratio $\le 0.30$, prediction gain $\ge 0.02$). Overall pass rate is zero on all three architectures: Autoware fails the $\rho$ and SPOF conditions, Cloud Microservices fails SPOF and prediction gain, and Train-Ticket fails SPOF. The SPOF condition fails everywhere ($F_1 = 0.50$, $0.33$, $0.57$), which matters disproportionately because single-point-of-failure attribution is the Availability half of the explanation layer's claim in Section~\ref{sec:5} and this is the only place in the study where that claim is tested directly against a system we did not generate. We report the gate outcomes rather than the ranking metrics alone because the gap between them is itself the finding: rank ordering transfers to these architectures, whereas the absolute, threshold-based judgements the framework would issue in CI/CD do not yet clear their own bars.

    \item \textbf{Predictive Advantage over Structural Heuristics:} The ``Gain vs.\ Deg'' column reports SaG's rank-correlation margin over an unweighted degree centrality baseline ($+0.357$ on Autoware, $+0.264$ on Train-Ticket, $+0.014$ on Cloud Microservices), consistent with heterogeneous message passing capturing multi-hop topic--broker cascades that degree centrality does not resolve. This margin is a difference between aggregate rank correlations and should not be read as a significance result: the paired per-node test recorded alongside it --- a Wilcoxon signed-rank test on $|\hat{Q}(v) - I^*(v)| - |\text{deg}(v) - I^*(v)|$ --- does not reach significance on any of the three systems ($p \ge 0.97$). That test compares raw score errors between quantities on different scales and is therefore weak evidence in either direction, but we report it rather than omit it. Evaluating \texttt{Topo-QoS} on these authentic systems was precluded because public open-source manifests lack declared fine-grained QoS contracts (such as durability and transport priorities); without synthetic QoS imputation, only unweighted structural degree could be evaluated directly, bounding this real-world evaluation to outperforming unweighted structural heuristics. Establishing a transfer advantage over QoS-weighted baselines on real-world systems remains an objective for future QoS-annotated benchmarks.
\end{enumerate}

\subsection{RQ5: Analysis Cost, Computational Sustainability, and CI/CD Feasibility}
\label{sec:7.5}

RQ5 quantifies computational overhead and sustainability during CI/CD evaluation, with per-stage latencies reported in Table~\ref{tab:scale}:

\begin{table}[htbp]
\centering
\small
\caption{Per-stage latency of the inference pipeline across scaling graph sizes (CPU, median of 3 runs).}
\label{tab:scale}
\begin{tabular}{rrrrrr}
\toprule
\textbf{$|V|$} & \textbf{$|E|$} & \textbf{Analyse (s)} & \textbf{Graph$\to$tensor (s)} & \textbf{HGT forward (ms)} & \textbf{Analyse : forward} \\
\midrule
249   & 1{,}127  & 0.27  & 0.011 & 22.5 & 12$\times$ \\
499   & 2{,}402  & 0.95  & 0.022 & 15.7 & 61$\times$ \\
999   & 6{,}422  & 4.74  & 0.055 & 36.7 & 129$\times$ \\
1{,}998 & 19{,}301 & 23.83 & 0.153 & 43.7 & \textbf{545$\times$} \\
\bottomrule
\end{tabular}
\end{table}

\noindent\textbf{The neural model is the cheapest stage, but not the whole cost.} At 2,000 components the HGT forward pass takes $43.7\,\text{ms}$ against $23.8\,\text{s}$ for deterministic structural analysis. That ratio locates cost \emph{inside} the pipeline; it is not the cost of evaluating an architecture. Indices 0--17 of every node feature vector (Section~\ref{sec:3.4}) --- betweenness, closeness, reverse PageRank, articulation and bridge scores --- are products of that same analysis stage, so the forward pass cannot run without it. End-to-end evaluation of an unseen 2,000-component architecture therefore costs about $24\,\text{s}$, of which the model is $0.2\%$; the $43.7\,\text{ms}$ figure is the marginal cost of re-scoring an already-analysed graph. Cost scales with edge density rather than node count: the 520-component Enterprise mesh needs $27.2\,\text{s}$ for its 3,245 structural edges, while a sparser 999-component system needs $4.7\,\text{s}$. Across the corpus the complete gate (structural analysis plus 18 anti-pattern detectors) runs in $0.02$--$27.4\,\text{s}$. Since the dominant stage is $O(|V|^2 + |V||E|)$, this budget should not be extrapolated: we have measured up to 2,000 components, and the systems invoked in Section~\ref{sec:1.1} can be an order of magnitude larger.

\noindent The quantity measured is wall-clock time, against multi-seed discrete-event simulation sweeps that are substantially more expensive on the same corpus. We deliberately do not convert it into an energy or carbon figure (Section~\ref{sec:8.2}). What the measurements support is the narrow claim that pre-deployment gating fits inside a pull-request budget at the scales we tested, without a simulation cluster.